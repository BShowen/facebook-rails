<h1>Posts</h1>

<% if @posts.any? %>
    <% @posts.each do |post| %>
        <article>
            <h2><%= post.title.capitalize %></h2>
            <p><%= post.body %></p>
        </article>
    <% end %>
<% else %>
    <h2>You dont have any posts yet.</h2>
    <%= link_to "Create a post", authenticated_root_path %>
<% end %>
